# @BotFather 에서 발급받은 봇 토큰 (필수)
TELEGRAM_TOKEN=

# 아래 값은 모두 선택 사항입니다. 비워 두면 기본값이 쓰입니다.
# SQLite DB 위치 (docker-compose 의 ./data 볼륨 안에 두세요)
# DB_PATH=/app/data/bot.db

# 공지 확인 주기(초)
# CHECK_INTERVAL_SECONDS=600

# 시간 표시 및 운영 기준 시간대
# TIMEZONE=Asia/Seoul
